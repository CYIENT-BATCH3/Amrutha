#include <stdio.h>

int main() {
    // Write C code here
    int a[5]={1,2,3,4,5};
    
  
printf("value of a=%d\n *a=%d\n",a,*a);//a means base address of array named a[5] and *a means value of array in a[0]
    return 0;
}